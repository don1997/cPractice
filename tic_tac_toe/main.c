#include<stdio.h>
#include"includes/functions.h"
int main(void)
{
    int selection;

    displayBoard();
    
    printf("Make a selection on the board: ");
    scanf("%d", &selection);

return 0;
}
